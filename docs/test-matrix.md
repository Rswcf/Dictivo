# Dictivo Test Matrix

Scope: local-first desktop UI, companion window, shared privacy contracts, and metadata-only API. The matrix maps current product behavior to automated coverage and remaining manual/E2E checks.

## Frontend Pages And States

| Area | Functional coverage | Edge/state coverage | Automated evidence |
| --- | --- | --- | --- |
| Dictation Workbench | Live transcript editor, start/stop button, dynamic hotkey chips, App-level language selector flow, App-level tier radio switch, resume-from-history shortcut, telemetry, hotkey and paste status, microphone capture controller, WAV encoding for local whisper.cpp, profile fallback | Engine not ready, long text and special characters, CJK character-count label, selected language passed to local dictation and persisted in history metadata, dictionary terms and snippet triggers filtered to the selected language for whisper prompt terms, snippet replacements kept out of whisper prompt terms and applied only during local polish, recording animation classes, paste failure after transcription, history-save failure after transcription, companion preview hidden/visible, accessible companion hide action, App-level companion hide and resume-to-history navigation, microphone permission denial, stop-before-microphone-controller-ready race, recorder error cleanup, WAV controller cleanup when setup fails or AudioContext shutdown fails, RIFF/WAVE header, 16 kHz mono PCM output, empty recording output, slow-profile failure fallback to Fast, Fast failure surfacing, slow warning | `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/componentsInteraction.test.tsx`, `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/mediaCapture.test.ts`, `apps/desktop/tests/localDictationEngine.test.ts`, `apps/desktop/tests/wireframeVisual.test.ts`, `apps/desktop/e2e/app.spec.ts` |
| Local History | Search input, per-message delete, per-message paste, clear-history confirmation, copy raw, copy final, markdown export | App-level copy/paste/delete/clear bridge calls update status and refresh list state, Markdown export file name/content through browser download, sanitized Markdown export filenames for legacy/corrupted session ids, separate no-history and no-search-results empty states, labelled search input, raw transcript present/missing, dense session metadata, CJK character-count labels, browser/native upsert consistency, corrupted browser history storage recovery, unsupported stored language/mode/provider/privacy filtering, clipboard permission denied fallback, 100-item cap, CJK and special-character persistence, destructive actions disabled during copy/paste/delete and operation locks prevent concurrent history mutations | `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/componentsInteraction.test.tsx`, `apps/desktop/tests/desktopBridge.test.ts`, `apps/desktop/tests/export.test.ts`, `apps/desktop/e2e/app.spec.ts`, `apps/desktop/src-tauri/src/storage.rs` |
| Dictionary & Snippets | App-level add/remove term and snippet state, language-scoped term/snippet views, add-term/add-snippet controls, duplicate/empty validation, removal controls | Empty dictionary, empty snippets, labelled term/snippet inputs, accessible remove buttons with term/trigger names, long term, duplicate term/trigger within the selected language, same trigger allowed across different languages, partial snippet form, URL replacement with query characters | `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/componentsInteraction.test.tsx`, `apps/desktop/tests/localPolish.test.ts` |
| Settings: Local Engine | Recommended card + Re-run setup, refresh status, three clickable tier cards (Fast / Medium / Quality), inline confirm for download / out-of-budget / model delete, Advanced collapse with raw catalog + tier mapping + import path | App-level refresh status and Medium re-run calibration bridge calls, startup tier-cache load failure feedback, App-level tier download/select/delete/import bridge calls, out-of-budget Quality warning -> download/benchmark/cache-write/activation flow, settings-launched setup wizard returns to Dictation after completion/skip, Active tier badge, "↓ Download" / "⚠ may be slow" hints, downloading busy overlay, no-model error, labelled model import select/path controls, empty/whitespace import path disabled, submitted import path trimmed, import failure status and operation-lock release, model select/download/delete/import actions, model operation lock across tier/import/catalog controls, failed tier activation rollback, non-Medium tier benchmark cache update without corrupting Medium calibration, "Run setup wizard instead" link | `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/modelManagerInteraction.test.tsx`, `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/desktopBridge.test.ts`, `apps/desktop/e2e/tier-cards.spec.ts` |
| Onboarding Wizard | Hardware scan, recommended model, download/setup, calibration, completion, skip | Hardware scan failure, optional GPU detection failure does not block setup, model catalog failure shows non-blocking status and recommended id fallback, model setup failure alert, calibration failure returns to retryable model step, explicit retry after failure, setup/download busy state disables skip, setup side effects stop after wizard unmount during download, Quality over-budget summary | `apps/desktop/tests/onboardingWizard.test.tsx`, `apps/desktop/e2e/onboarding.spec.ts` |
| Settings: Hotkeys | Dictation and Paste Last shortcut rows, shortcut recorder, activation mode select, main workbench shortcut chips | Toggle/hold mapping, duplicate shortcut collapse, duplicate assignment prevention, Escape cancel, Command/Control/Alt-required validation with Shift-only rejection, changed shortcut and hold/toggle mode immediately reflected on Dictation quick tips and capture hint, press/release behavior, hold-key repeat suppression before release, paste-last success from latest history, paste-last failure feedback, late native registration cleanup when the App unmounts or hotkey settings change before `register()` resolves | `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/settingsInteraction.test.tsx`, `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/hotkeys.test.ts` |
| Settings: Engine → Advanced → Processing | Auto polish, spoken punctuation, filler removal, capitalization toggles (collapsed under Engine → Advanced) | App-level toggle changes passed into local dictation options, auto-polish disabled, CJK punctuation, snippets with URLs, dictionary capitalization, default Message output | `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/settingsInteraction.test.tsx`, `apps/desktop/tests/localPolish.test.ts` |
| Settings: Companion | Enable toggle, avatar picker | Dog/cat/Trump/bikini/muscle avatar normalization, hidden companion setting persistence | `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/settingsInteraction.test.tsx`, `apps/desktop/tests/settingsStore.test.ts`, `apps/desktop/tests/companion.test.ts` |
| Settings: Privacy | App-level system settings bridge + privacy refresh, permission cards, macOS Accessibility status detection, browser microphone permission merge, status detail copy, system settings launch buttons, app version, refresh control | Granted, denied, not-required, clipboard-only, web-preview, unknown native placeholder, non-actionable statuses do not show settings buttons, non-Tauri settings launch blocked, browser microphone permission API unavailable, Open settings success and failure statuses, refresh removes resolved permission actions, macOS / Windows / Linux permission settings command targets locked by Rust pure-function tests | `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/privacySettings.test.ts`, `apps/desktop/tests/componentsStatic.test.tsx`, `apps/desktop/tests/settingsInteraction.test.tsx`, `apps/desktop/tests/desktopBridge.test.ts`, `apps/desktop/src-tauri/src/lib.rs` |
| Floating Companion | Sidebar mascot opens native companion window, idle/recording/processing/complete/blocked/error summaries, main-window position calculation, native-window render shell, companion hide event sync | Timer source, word count, paste status, avatar asset, event-driven state updates, drag start, hide request, main-window setting/status sync after hide request, bitmap/SVG avatar variants, secondary monitor origins, small work areas, unavailable-window status, grayscale wireframe styling, native transparent/always-on-top/hidden-start config contract | `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/companion.test.ts`, `apps/desktop/tests/companionWindow.test.tsx`, `apps/desktop/tests/wireframeVisual.test.ts`, `apps/desktop/tests/version.test.ts` |
| Desktop UI | Sidebar layout, single-column panels, capture stage centering, labelled language selector, keyboard focus visibility, horizontal overflow guard, nonblank screenshot capture | Fixed 72px sidebar, startup history-load failure feedback, startup native-refresh failure feedback, 1440×960 chromium baseline, tab navigation through primary nav, dictation controls, history actions, dictionary/snippet inputs, settings tabs, tier cards, and inline confirmations; Playwright fails on browser `pageerror`, `console.error`, and non-local network/WebSocket requests | `apps/desktop/tests/wireframeVisual.test.ts`, `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/e2e/app.spec.ts`, `apps/desktop/e2e/fixtures.ts` |

## Core Logic And API

| Area | Functional coverage | Failure/edge coverage | Automated evidence |
| --- | --- | --- | --- |
| Local bridge fallback | Web preview permission states, Tauri runtime microphone permission merge, model catalog, hardware estimate, clipboard fallback, local history storage fallback, private-fast transcription bridge, native hardware/tier/benchmark/model-operation bridge | No `window` runtime, clipboard API denied fallback, browser microphone permission API unavailable, corrupted history JSON cleanup, malformed history record and unsupported enum filtering, native transcription blocked outside Tauri, native clipboard marker / paste / copy commands forwarded with exact payloads, native hardware/profile/GPU/runnable-tier/benchmark/select/download/import/delete commands forwarded with exact parameters inside Tauri, calibration fallback builds web-preview runnable tiers outside Tauri, non-WAV audio rejected before native invoke, WAV audio base64 + dictionary/snippet-trigger prompt terms passed to native command without snippet replacement values | `apps/desktop/tests/desktopBridge.test.ts` |
| Local polishing | Punctuation, snippets, fillers, capitalization, dictionary terms, default Message output | URL snippets preserved, CJK punctuation without inserted spaces, legacy raw/email/prompt formatters retained at the pure-function layer but no longer exposed as primary UI modes | `apps/desktop/tests/localPolish.test.ts` |
| Settings migration | Legacy cloud settings removed, local-only settings retained | Invalid profile/tier/language/mode/avatar/hotkey/toggle fallback, legacy hotkey upgrade, unsafe or duplicate stored hotkey normalization, corrupted v4 settings recovery, malformed dictionary/snippet record filtering and language-aware dedupe | `apps/desktop/tests/settingsStore.test.ts` |
| Shared privacy contract | Forbidden content fields detected recursively | Metadata-only payload allowed, only local provider exported | `packages/shared/tests/privacy.test.ts` |
| API privacy guard | Metadata API rejects transcript content, common content aliases, and legacy cloud provider; billing checkout/webhook also reject forbidden content fields | Valid local-only session accepted; nested forbidden fields report precise paths; alias fields such as `content`, `rawText`, `transcript_text`, and `prompt_terms` are rejected before route schemas can strip unknown keys; forbidden-field matching is case/punctuation-format insensitive | `apps/api/src/routes/privacy.test.ts`, `packages/shared/tests/privacy.test.ts` |
| API metadata routes | Health, entitlements, usage, billing scaffolding | Stripe missing uses mock checkout; DB absent returns safe defaults; invalid usage and checkout inputs; transcription, usage, and checkout schemas reject unknown fields instead of silently stripping them | `apps/api/src/routes/metadata.test.ts` |
| API security controls | Configured-origin CORS, global rate limit, 64 KB metadata body limit, Stripe webhook signature verification, test log silence, browser-preview no-external-network gate | Preflight headers, repeated request 429, oversized body 413, missing/invalid/expired/valid `stripe-signature`, redacted non-test logger config, Playwright failure on non-local requests | `apps/api/src/routes/security.test.ts`, `apps/api/src/index.ts`, `apps/api/src/routes/billing.ts`, `apps/desktop/e2e/fixtures.ts` |
| Native Tauri layer | Rust unit tests, packaged whisper.cpp CLI smoke, installed app metadata smoke, tray lifecycle action mapping, macOS universal app workflow contract, Windows MSI/NSIS workflow contract, global shortcut capability, minimum Tauri permission surface, Rust global-hotkey probe | Storage isolation, model cleanup on transcription failure, benchmark output cleanup, unsupported model id rejection, user transcription failure errors do not echo whisper stdout/stderr, installed macOS version and microphone/AppleEvents usage descriptions verified, smoke script transcript/metadata/model-scan assertions covered without launching real whisper, close-to-hide and tray show/hide/quit routing covered, app identity and bundled private-fast resources aligned with version metadata, bundled Private Fast manifest points at a present CLI binary, benchmark WAV is RIFF/WAVE 16 kHz mono 16-bit PCM with data, Private Fast prepare script clears stale manifest/macOS binary/Windows binary/DLL artifacts before copying a platform build, release workflow runs dependency audit/Rust format/whitespace gates before packaging, release workflow token permissions locked to read-only, companion native window config locked to transparent/borderless/always-on-top/hidden-start/no-focus/no-shadow, companion event listeners clean up even if async Tauri listener registration resolves after unmount, global hotkey registration unregisters again if async native registration resolves after cleanup, fails availability checks, or native registration rejects, macOS universal matrix target/artifact/gate order covered, Windows child commands use `CREATE_NO_WINDOW` for paste/settings/private-fast paths, macOS / Windows / Linux permission settings commands locked by platform-independent Rust tests, Windows x64 MSI + NSIS matrix target/artifact/gate order covered, Windows NSIS installer locked to current-user mode, exact Tauri capability allowlist locked and unused opener plugin/capability absent, interactive global-hotkey probe kept out of automatic push/tag CI and available as manual workflow opt-in; 2026-05-13 macOS interactive probe passed locally for the default shortcuts | `apps/desktop/tests/version.test.ts`, `apps/desktop/tests/hotkeys.test.ts`, `apps/desktop/tests/releaseWorkflow.test.ts`, `apps/desktop/tests/appStartup.test.tsx`, `apps/desktop/tests/companionWindow.test.tsx`, `apps/desktop/src-tauri/capabilities/default.json`, `apps/desktop/src-tauri/Cargo.toml`, `apps/desktop/src-tauri/src/storage.rs`, `apps/desktop/src-tauri/src/private_fast.rs`, `apps/desktop/src-tauri/src/lib.rs`, `.github/workflows/build-desktop.yml`, `scripts/prepare-private-fast-engine.mjs`, `scripts/smoke-private-fast.mjs`, `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml`, `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --test global_hotkey_probe -- --ignored --nocapture`, `npm run smoke:private-fast` |

## Manual And E2E Scenarios

These require a real browser/Tauri runtime, microphone permissions, and a local model. The detailed release checklist lives in `docs/native-manual-test-plan.md`.

| Scenario | Steps | Expected result |
| --- | --- | --- |
| Real dictation happy path | Install/import model, start recording, speak, stop | WAV audio transcribes locally, transcript is polished, saved, copied/pasted, history updates |
| Microphone denial / setup race | Deny mic permission and start dictation; also start then immediately stop before the OS/microphone controller finishes setup | Error status is shown, stale recording placeholder is cleared, companion enters attention state, no session is saved on denial. Stop-before-ready is queued until the controller exists, then the recording is stopped/transcribed without a dangling controller. App-level denial and setup-race handling are automated; real OS denial still needs manual verification. |
| Clipboard race | Change clipboard between recording and paste | Transcript stays in app and is not pasted over changed clipboard. App-level `clipboard-changed-copied` handling is automated; real cross-app clipboard mutation still needs manual verification. |
| Native model operations | Download/select/delete/import every listed model | Operation state disables duplicate clicks, low-disk download/import fails before network transfer or file copy, and final status updates correctly |
| Global hotkeys | Toggle and hold modes from another active app | Start/stop/paste-last intents fire exactly once per press/release sequence. Default shortcut registration passed the macOS native probe; cross-app behavior still needs manual verification. |
| Rapid navigation | Click all nav/settings tabs repeatedly during loading/operation | No UI state loss, overlap, or disabled-control escape |
| Browser sizes | 1440×960 chromium desktop is automated; tablet / mobile are manual spot checks since Dictivo is a Tauri desktop app | 1440px desktop covered by Playwright; smaller viewports are not a release-blocking surface |
| Accessibility pass | Keyboard tab through main nav, forms, settings, companion close | Visible focus outline, named icon buttons, no keyboard trap. Browser-preview keyboard traversal is automated in `apps/desktop/e2e/app.spec.ts`; packaged native keyboard pass remains a release checklist item. |

## Hardware Tier Mapping (manual)

For each row, install Dictivo on the target hardware, run the onboarding wizard, and record what the UI shows. All three tier cards (Fast / Medium / Quality) always render now — entries flagged ⚠ are still clickable but warn before download. Confirm the workspace footer status chips show the active tier + accel + model, and that the Companion floating window paints with no window chrome.

| Machine                              | Expected `performance_class` | Expected tiers visible            | Verified |
| ------------------------------------ | ---------------------------- | --------------------------------- | -------- |
| macOS Apple Silicon M3 / 16 GB       | `GpuHigh`                    | Fast, Medium, Quality                 |          |
| macOS Apple Silicon M1 / 8 GB        | `CpuStrong`                  | Fast, Medium (Quality flagged ⚠)      |          |
| macOS Intel 16 GB with AMD dGPU 8 GB | `GpuHigh`                    | Fast, Medium, Quality                 |          |
| macOS Intel 16 GB, integrated GPU    | `CpuStrong`                  | Fast, Medium (Quality flagged ⚠)      |          |
| Windows + RTX 3060 (8 GB) + 16 GB    | `GpuHigh`                    | Fast, Medium, Quality                 |          |
| Windows CPU-only, 8 cores, 16 GB     | `CpuStrong`                  | Fast, Medium (Quality flagged ⚠)      |          |
| Windows CPU-only, 4 cores, 8 GB      | `CpuWeak`                    | Fast (Medium / Quality flagged ⚠)     |          |
| Linux + NVIDIA CUDA + 16 GB          | `GpuHigh`                    | Fast, Medium, Quality                 |          |
| Linux CPU-only, 8 cores, 16 GB       | `CpuStrong`                  | Fast, Medium (Quality flagged ⚠)      |          |

## Coverage Limits

The repository runs Playwright E2E coverage on a 1440×960 chromium-desktop project plus Vitest coverage for render contracts, pure logic, fallback behavior, CSS contracts, API privacy, and release workflow contracts. Playwright now fails tests on browser `pageerror`, `console.error`, and non-local network/WebSocket requests, so UI flows cannot pass with hidden runtime exceptions or accidental external calls. The previous chromium-mobile project was retired since Dictivo ships as a Tauri desktop app; tablet/mobile viewport behavior is a manual spot check. The suite still cannot prove native microphone capture, real OS permission dialogs, global shortcut registration in another app, packaged tray clicks, packaged companion chrome/always-on-top behavior, packaged native keyboard behavior, or Windows install/hotkey behavior without running the packaged desktop app with local permissions and target OS environments. Those are tracked above as manual/native scenarios.
