# v1 reference (over-scoped — do not use as plan)

The 9 docs in this directory were the first pass and assumed a company-scale operation: custom Cloudflare Workers for license issuing and update gating, a separate verify endpoint, Resend/Postmark for email, a /vs comparison-page suite, a founder-Lifetime tier, multi-environment beta channel, counsel review before launch.

That is more than a solo indie selling a $49 desktop app needs.

The lean plan lives in `../plan.md`. These files are kept only for ideas worth promoting *later* (post $10k revenue, when scale forces real backend work).
